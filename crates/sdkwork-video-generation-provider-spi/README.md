# SDKWork Video Generation Provider SPI

Transport-neutral L3 provider contracts for video generation. This crate contains no SDK routes,
generated SDK DTOs, raw HTTP, credentials, or runtime configuration.
