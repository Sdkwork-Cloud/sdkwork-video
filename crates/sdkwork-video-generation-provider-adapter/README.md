# SDKWork Video Generation Provider Adapter

L4 adapter that exclusively owns generated SDK route selection, SDK DTO construction, vendor
parameter decoding, SDK error mapping, and provider response normalization.
