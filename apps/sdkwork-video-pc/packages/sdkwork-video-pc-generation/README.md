# @sdkwork/video-pc-generation

Video generation workspace UI for PC surfaces.

## Exports

- `@sdkwork/video-pc-generation/react` — `VideoGenerationWorkspaceView`

## Workspace composition

Delegates to `@sdkwork/assets-pc-assets/generation-playground-workspace` with `modality="video"` and `bucket="videos"`.

## Verification

```bash
pnpm --filter @sdkwork/video-pc-generation typecheck
```
